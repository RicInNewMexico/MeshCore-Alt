name: Build And Publish Firmware Artifacts (STM32)

permissions:
  contents: read

on:
  workflow_dispatch:
  push:
    branches:
      - main

jobs:
  build-and-publish:
    uses: ./.github/workflows/build-and-publish-firmware-artifacts.yml
    with:
      platform_filter: STM32_PLATFORM
      workflow_label: STM32
    secrets: inherit
